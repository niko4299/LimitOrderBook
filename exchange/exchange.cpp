#include "exchange.hpp"

Exchange::Exchange(const std::vector<std::string>& symbols, std::size_t ringbuffer_size_per_instrument) {
    for(auto i = 0; i < symbols.size(); i++){
        // auto orderbook = std::make_shared<OrderBook>();

    }
};